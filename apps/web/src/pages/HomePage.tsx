import { Layout } from "../components/layout/Layout"
import { WelcomePage } from "../features"

export const HomePage = () => {
    return (
        <Layout>
            <WelcomePage />
        </Layout>
    )
}